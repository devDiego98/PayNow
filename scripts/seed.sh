#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
# npx prisma db seed — enable when seed script exists in package.json
echo "Seed script placeholder — implement prisma/seed.ts and package.json prisma.seed"
